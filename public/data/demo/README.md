# Demo week-one packet — SYNTHETIC

Generated by `organvm/daily-engine` from its committed synthetic example
intake against the committed rob-fitness starter pack:

    python3 tools/render_week.py \
      --intake instances/rob-fitness/example-intake.json \
      --start 2030-06-02 --out <dir>

Deterministic: re-running that command reproduces this file byte-for-byte.
No real client anywhere in it — the intake is synthetic, the start date is
deliberately in the future, and the pack is provenance-sanitized.

**Real client packets are NEVER committed to any repo.** The Field Ops panel
loads them locally (drag-drop / file picker, in-browser only — nothing is
uploaded, nothing is stored). Regenerate this demo whenever the engine's pack
changes.
